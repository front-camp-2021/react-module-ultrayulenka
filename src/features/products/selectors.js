export const selectProducts = (state) => state.products.items;

export const selectLoadingStatus = (state) => state.products.loading;

export const selectErrorStatus = (state) => state.products.error;
